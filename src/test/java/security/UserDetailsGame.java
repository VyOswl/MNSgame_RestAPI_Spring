package security;

import org.springframework.security.core.authority.SimpleGrantedAuthority;

public class UserDetailsGame {
    /*Optional<GameMaker> gameMaker = gameMakerDao.findById(utilisateur.getId());
    if(gameMaker.isPresent()){
        listAuthority.add(new SimpleGrantedAuthority((role:"ROLE_Game_Maker"));
    }*/
}
